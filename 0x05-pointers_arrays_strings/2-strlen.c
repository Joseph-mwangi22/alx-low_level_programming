#include "main.h"
#include <stdio.h>

/**
 *_strlen - returns the length of a string.
 * @s: the string to get the length of
 * return: the length of @s.
 */

int _strlen(char *s)
{
	int s  = 0;

	while (*s != '\0')
	{
		length++;
		s++;
	}
	return (length);
}
